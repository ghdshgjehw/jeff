{
  "name": "admin-chat",
  "version": "1.0.0",
  "description": "Real-time chat where an admin can message users individually, but users cannot message each other.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "socket.io": "^4.7.5",
    "nanoid": "^3.3.7",
    "resend": "^4.0.1"
  },
  "engines": {
    "node": ">=22.5.0"
  }
}